{
  "name": "realeffectmedia-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "import-squarespace": "node scripts/import-squarespace.mjs",
    "deploy": "astro build && wrangler pages deploy dist --project-name realeffectmedia --branch main",
    "preview:cf": "wrangler pages dev dist --port 8788"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.5.0",
    "@fontsource-variable/archivo": "^5.2.0",
    "@fontsource/anton": "^5.2.0",
    "@fontsource/ibm-plex-mono": "^5.2.0",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "turndown": "^7.2.0",
    "wrangler": "^4.131.0"
  }
}
